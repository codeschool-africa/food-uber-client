import React from "react"

import "./profileModal.sass"

const ProfileModal = ({ user, closeModal }) => {
  let { name, email, address, location, tel, dp } = user
  return <div></div>
}

export default ProfileModal
